# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(time-tick STATIC 
  time_tick.c)

include(Tornado.cmake)
set_tornado(time-tick)

target_include_directories(time-tick PUBLIC ../include)


target_link_libraries(time-tick PUBLIC 
  monotonic-time
  clog)

